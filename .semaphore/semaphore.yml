version: v1.0
name: Run in Docker
agent:
  machine:
    type: r1-standard-4
    os_image: ubuntu2204

blocks:
  - name: run-wg
    task:
      jobs:
        - name: run wg-easy
          commands:
            - checkout
            - docker compose up -d
